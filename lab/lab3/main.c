#include <sys/stat.h>
#include <fcntl.h>
#include <unistd.h>

#include <stdio.h>

int main(int argc, const char * argv[]) {
    for (int i = 0; i < argc; ++i) {
        printf("%s\n",argv[i]);
        int fd = open(argv[i], O_RDONLY|O_CREAT|O_EXCL, 0752);
        if (fd == -1) {
            perror("hahahahhahaa");
        } else {
            printf("%i:\n", fd);
            close(fd);
        }
    }
    return 0;
}

































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































